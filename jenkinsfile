pipeline{
    agent any
    stages{
        stage("initial"){
            steps{
                sh '''
                node --version
                docker --version
                '''
            }
        }
    }
}